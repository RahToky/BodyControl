import 'package:flutter/material.dart';

class ProgressListPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('list progress'),);
  }
}
